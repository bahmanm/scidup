# libscidup-database

`libscidup-database` is the database layer for SCID-family storage, index metadata, name tables, filters, search, tree statistics and codecs. It depends on `ScidUp::Core` and `Threads` only; opening-book ECO classification and application workflows live outside this library.
