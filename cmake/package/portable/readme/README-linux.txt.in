ScidUp - The Cross-Platform Chess Database and Analysis GUI

Version          @SCIDUP_RELEASE_VERSION@
For              @SCIDUP_RELEASE_PLATFORM@
Released On      @SCIDUP_RELEASE_DATE_LINE@

--------------------------------------------------------------------------------

1️⃣ How to install

1. Create a directory where you want ScidUp to live; "installation" means just
   moving this directory.
2. Extract the archive into that directory.

For instance, assuming you want to install ScidUp in $HOME/ScidUp:
        mkdir -p $HOME/ScidUp
        tar -xzf <archive>.tar.gz -C $HOME/ScidUp

--------------------------------------------------------------------------------

2️⃣ How to run

In a terminal, assuming you installed ScidUp in $HOME/ScidUp:
        $HOME/ScidUp/bin/scidup

If it does not start, ensure the binary is executable:
        chmod +x $HOME/ScidUp/bin/scidup

--------------------------------------------------------------------------------

3️⃣ Where are my settings?

ScidUp stores settings under:
- $XDG_CONFIG_HOME/scidup (when XDG_CONFIG_HOME is set)
- ~/.config/scidup (otherwise)

You can also set SCIDUP_CONFIG_HOME to use a custom settings directory.

--------------------------------------------------------------------------------

4️⃣ Something broken? Report it, please!

- Create a new issue: https://github.com/bahmanm/scidup/issues/new
- See existing issues: https://github.com/bahmanm/scidup/issues

When reporting a bug, please include:
- Linux distribution and version
- The ScidUp release
- Any terminal output

--------------------------------------------------------------------------------

5️⃣ License

ScidUp is distributed under the GNU GPL v2 (see COPYING).
Unless stated otherwise, modifications and additions in this fork are © their
respective contributors (see Git history) and licensed under the same terms.

--------------------------------------------------------------------------------

May Caissa be with you!
